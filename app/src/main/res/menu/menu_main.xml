<menu xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <!-- Deleted for using in custom Bar -->
    <!--
    <item android:id="@+id/more_action"
        android:title="@string/more"
        android:icon="@drawable/ic_list_black_36dp"
        android:showAsAction="ifRoom"
        />
     -->
    <item android:id="@+id/alarm_list_action"
        android:title="@string/alarms"
        android:icon="@drawable/ic_alarm_black_36dp"
        android:showAsAction="always" />
</menu>
